# DeepSeek API key (required for data generation + API judge)
DEEPSEEK_KEY=sk-your-key-here

# Optional: HuggingFace token for gated models
HF_TOKEN=hf_optional

# Optional: Anthropic API key (not used in current experiments)
ANTHROPIC_AUTH_TOKEN=sk-optional
